""""""""""Main configuration (VIM)"""""""""" 

set noesckeys        " allows using esc keys in insert mode - for macro recording
set nocompatible     " disable vi compatibility - this is required by many plugins 
